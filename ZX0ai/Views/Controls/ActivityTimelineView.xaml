<UserControl
    x:Class="ZX0ai.Views.Controls.ActivityTimelineView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ZX0ai.Models"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="ActivityItemTemplate" x:DataType="models:ActivityEntry">
            <Grid Margin="6,6,6,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="36" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Icon (vector Path from resources) -->
                <Viewbox Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Top">
                    <ContentControl Content="{Binding IconKey}" />
                </Viewbox>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0">
                    <TextBlock Text="{Binding Title}" FontFamily="{StaticResource AppFontFamily}" FontSize="13" Foreground="{ThemeResource InkBrush}" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding Details}" FontFamily="{StaticResource AppFontFamily}" FontSize="12" Foreground="{ThemeResource InkTertiaryBrush}" TextWrapping="Wrap" />

                    <!-- Terminal / Output area (collapsed unless output present) -->
                    <Border Margin="0,6,0,0" Background="{ThemeResource SunkenBrush}" CornerRadius="6" Padding="8" Visibility="{Binding Output, Converter={StaticResource StringToVisibilityConverter}}">
                        <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollMode="Auto" Height="120">
                            <TextBlock Text="{Binding Output}" FontFamily="Cascadia Code, Consolas" FontSize="12" TextWrapping="WrapWholeWords" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding TimestampText}" FontFamily="{StaticResource AppFontFamily}" FontSize="11" Foreground="{ThemeResource InkTertiaryBrush}" />
                    <TextBlock Text="{Binding State}" FontFamily="{StaticResource AppFontFamily}" FontSize="11" Foreground="{ThemeResource InkSecondaryBrush}" HorizontalAlignment="Right" />
                </StackPanel>

                <!-- Entrance animation when the container loads -->
                <Grid.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" From="0" To="1" Duration="0:0:0.18" />
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-6" To="0" Duration="0:0:0.18" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Grid.Triggers>

                <Grid.RenderTransform>
                    <TranslateTransform Y="0" />
                </Grid.RenderTransform>
            </Grid>
        </DataTemplate>

    </UserControl.Resources>

    <Grid>
        <ListView x:Name="ActivityListView" ItemsSource="{x:Bind ViewModel.Activities, Mode=OneWay}" ItemTemplate="{StaticResource ActivityItemTemplate}" SelectionMode="None" IsItemClickEnabled="False" />
    </Grid>
</UserControl>